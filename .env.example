DB_HOST=localhost
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=

# run `openssl rand -base64 32` to generate JWT_SECRET
JWT_SECRET=

# run `openssl rand -base64 32` to generate ADMIN_API_KEY
ADMIN_API_KEY=

SERVER_PORT=

# Frontend (VITE_ prefix required for Vite to expose to the browser)
VITE_API_BASE_URL=http://localhost:8080/api
VITE_ADMIN_API_KEY=
VITE_TOKEN_SECRET=